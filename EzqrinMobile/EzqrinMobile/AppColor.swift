import SwiftUI

extension Color {
    enum app {
        static let success       = Color("AppSuccess")
        static let warning       = Color("AppWarning")
        static let destructive   = Color("AppDestructive")
        static let statusOngoing = Color("AppStatusOngoing")
    }
}
